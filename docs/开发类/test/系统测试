这是一份基于你提供的《GIMP_AI_Mentor软件设计说明书(SDD)_v1.docx》生成的详细系统测试计划和测试程序。

---

# **GIMP AI Mentor (智能修图指导工具) 系统测试计划与测试程序**

**文档版本：** V1.0
**基于文档：** SDD (软件设计说明书) v1
**测试对象：** GIMP AI Mentor Python-Fu 插件
**当前环境参考：** Windows 10/11, GIMP 2.10.34, Python 3.8.10

---

## **1. 概述**

### **1.1 系统需求概述**
本系统旨在开发一款基于Python-Fu的GIMP插件，核心目标是利用大语言模型（LLM）技术，将GIMP复杂的操作逻辑转化为自然语言交互，充当用户的“AI导师”。系统需实现以下核心价值：
*   **降低门槛：** 通过自然语言输入修图需求，自动生成操作指南。
*   **技能习得：** 通过可视化的步骤引导，帮助用户理解并掌握专业技法（“白盒化引导”）。
*   **非破坏性编辑：** 所有修改默认在临时图层进行，确保原始数据安全。
*   **工作流复用：** 支持将修图流程保存为本地“配方”（Recipe）进行固化和分享。

### **1.2 系统设计与实现概述**
系统采用分层插件架构，作为GIMP的原生Python插件运行。
*   **架构模式：** 分层架构（表现层/业务逻辑层/AI交互层/GIMP集成层）。
*   **核心驱动：** 状态机模式（State Machine）管理引导流程。
*   **关键技术：** 多线程异步处理（避免阻塞主线程）、非破坏性编辑内核（调整图层/蒙版）、结构化JSON指令解析。
*   **数据流向：** GIMP宿主环境 -> 图像/状态读取 -> 云端AI API -> 结构化指令 -> 本地执行/预览 -> 用户确认/撤销。

---

## **2. 测试计划**

### **2.1 测试环境**
根据SDD 2.3章节，测试环境需满足以下要求：

| 环境类型 | 配置项 | 详细要求 |
| :--- | :--- | :--- |
| **硬件环境** | 处理器 | Intel Core i5-10400 或 AMD Ryzen 5 5600 及以上 |
| | 内存 | 16GB DDR4 (测试峰值内存占用需 < 500MB) |
| | 存储 | 512GB SSD，可用空间 > 10GB |
| | 网络 | 带宽 ≥ 100Mbps，上行 ≥ 10Mbps (模拟AI请求) |
| | 显示器 | 1920x1080 分辨率，DPI缩放 100%-200% |
| **软件环境** | 宿主软件 | GIMP 2.10.34 |
| | 运行时 | Python 3.8.10 |
| | 操作系统 | Windows 10/11, macOS 10.15+, Ubuntu 20.04+ |
| **测试工具** | 网络模拟 | Charles/Fiddler (模拟API延迟、超时、Mock数据) |
| | 性能监控 | Task Manager/Activity Monitor (内存/CPU监控) |

### **2.2 测试需求IADT分解**
基于SDD功能设计与接口设计，分解如下测试维度：

1.  **功能交互测试 (Interaction)：** 验证UI操作与GIMP宿主的联动（如选区感知、图层复制）。
2.  **AI数据流转测试 (Data Flow)：** 验证图像Base64编码、Prompt拼接、JSON解析的准确性。
3.  **指令执行测试 (Action)：** 验证JSON指令映射为GIMP PDB调用的正确性（如色阶、色彩平衡）。
4.  **非破坏性测试 (Non-Destructive)：** 验证预览图层机制、撤销/重做历史栈的完整性。
5.  **异常与安全测试 (Exception)：** 网络超时降级、恶意配方文件校验、API密钥管理。

### **2.3 测试实施**
*   **阶段一（冒烟测试）：** 验证插件加载、面板显示、基础配置读写（SDD 4.1, 4.3.4）。
*   **阶段二（核心功能测试）：** 覆盖“意图输入 -> AI分析 -> 步骤引导 -> 预览执行”的全链路（SDD 3.1, 3.2）。
*   **阶段三（配方与管理测试）：** 验证配方的导入、导出、内置预设加载（SDD 3.3）。
*   **阶段四（非功能测试）：** 性能指标验证（SDD 6.2）、兼容性测试（SDD 7.5）、安全性扫描。
*   **阶段五（回归测试）：** 修复缺陷后，针对相关模块进行回归验证。

### **2.4 通过准则**
1.  **功能通过率：** 核心功能用例（P0级）通过率必须达到 100%。
2.  **缺陷等级：** 不允许存在严重（Critical）和一般（Major）缺陷。
    *   *Critical：* 插件崩溃、GIMP卡死、内存泄漏导致系统重启。
    *   *Major：* AI指令执行错误导致图像损坏、核心引导流程中断。
3.  **性能达标：** UI响应延迟 ≤ 300ms，全链路AI请求 ≤ 30秒（含网络），内存峰值 ≤ 500MB。
4.  **数据安全：** 严格遵循非破坏性原则，原始图层在任何操作下均不可被直接修改。

---

## **3. 测试程序**

### **3.1 测试程序组成**
本测试程序包含以下五个部分：
1.  **功能测试：** 验证业务逻辑的正确性。
2.  **性能测试：** 验证资源占用与响应速度。
3.  **接口测试：** 验证内部模块与外部服务的通信。
4.  **文档与介质一致性测试：** 验证安装包与手册的一致性。
5.  **自由操作测试（探索性测试）：** 模拟用户随机操作。

---

### **3.2 功能测试**

**测试目标：** 验证SDD第3章定义的核心功能。

| 序号 | 需求规格 (SRS/SDD) | 操作步骤 | 期望输出 | 实际输出 | 通过与否 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **F-01** | **多模态输入与选区感知** (SDD 3.1.1) | 1. 在GIMP中打开图片并创建一个矩形选区。2. 打开插件面板，在输入框输入“优化选区色彩”。3. 点击“智能分析”。 | 1. 状态栏显示“当前模式：局部优化”。2. 发送给AI的Prompt中包含 `focus_area: selection` 元数据。3. AI分析结果针对选区部分给出建议。 | | |
| **F-02** | **诊断报告展示** (SDD 3.1.2) | 1. 上传一张曝光不足的图片。2. 触发自动诊断或手动分析。 | 1. UI显示结构化卡片。2. 内容包含：“检测到曝光不足”、“建议提升亮度”。3. 严重程度标记为“高”。 | | |
| **F-03** | **非破坏性预览** (SDD 3.2.2) | 1. 接受AI生成的“提亮”步骤。2. 观察图层面板。3. 点击插件内的“撤销预览”。 | 1. GIMP图层面板自动创建名为 `[AI Mentor Preview]` 的新图层。2. 原图层保持锁定/未被修改。3. 撤销后，预览图层被删除，原图恢复如初。 | | |
| **F-04** | **分步高亮指引** (SDD 3.2.3) | 1. 开始执行一个包含“色彩平衡”的步骤。2. 观察插件UI及GIMP界面。 | 1. 插件UI显示浮动指引窗口，高亮“色彩平衡”工具图标。2. 显示文字提示：“请点击 菜单->颜色->色彩平衡”。3. GIMP原生界面无强制Hook，指引层覆盖在上方。 | | |
| **F-05** | **配方导入/导出** (SDD 3.3.1) | 1. 完成一次修图后，点击“保存配方”。2. 退出插件，点击“导入配方”。3. 选择刚才保存的 `.gimp-ai-recipe` 文件。 | 1. 文件成功保存至本地目录。2. 导入时进行格式校验，非法JSON提示“文件损坏”。3. 导入成功后，步骤列表自动加载该配方的参数。 | | |
| **F-06** | **网络超时降级** (SDD 3.4.1) | 1. 使用抓包工具拦截AI请求。2. 模拟服务器延迟 > 35秒。3. 观察插件状态。 | 1. 30秒后UI提示“网络请求超时”。2. 自动触发重试机制（1次）。3. 重试失败后，提供“离线演示模式”按钮，加载Mock数据供体验。 | | |

---

### **3.3 性能测试**

**测试目标：** 验证SDD第6.2章节定义的关键性能指标。

| 序号 | 需求规格 (SRS/SDD) | 操作步骤 | 期望输出 | 实际输出 | 通过与否 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **P-01** | **UI响应延迟** (SDD 6.2.2) | 1. 在插件运行时，频繁点击“切换预览”、“取消选中”等操作。2. 监控主线程卡顿情况。 | 1. 任何UI操作（点击/拖动）响应时间 ≤ 300ms。2. 无界面冻结（Hanging）现象。 | | |
| **P-02** | **全链路耗时** (SDD 6.2.2) | 1. 准备一张10MB的JPG图片。2. 点击“智能分析”并生成策略。3. 记录从点击到步骤列表显示的总时间。 | 1. 总耗时 ≤ 30秒（包含网络传输）。2. 其中AI请求等待时间 ≤ 30秒，本地解析 ≤ 500ms。 | | |
| **P-03** | **内存占用** (SDD 6.2.1) | 1. 打开任务管理器监控Python进程。2. 连续执行5次“分析->预览->撤销”循环。3. 操作结束后，关闭插件面板。 | 1. 峰值内存占用 ≤ 500MB。2. 关闭插件后，内存释放回初始水平（无泄漏）。3. 大图（>20MB）上传前有拦截提示。 | | |
| **P-04** | **预览更新帧率** (SDD 6.2.2) | 1. 在参数调整滑块上进行快速拖动。2. 观察预览图更新情况。 | 1. 预览图更新延迟 ≤ 500ms。2. 滑块拖动流畅，无明显卡顿跳跃。 | | |

---

### **3.4 接口测试**

**测试目标：** 验证SDD第4章定义的内外部接口。

| 序号 | 需求规格 (SRS/SDD) | 操作步骤 | 期望输出 | 实际输出 | 通过与否 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **I-01** | **云端API通信** (SDD 4.3.3) | 1. 拦截 `/v1/analyze` 请求。2. 检查请求头、Body结构。3. 模拟返回错误JSON。 | 1. 请求Header包含 `Authorization`。2. Body包含 `image_base64` 和 `messages` 结构。3. 错误JSON被正确捕获，UI提示“AI返回数据异常”，不崩溃。 | | |
| **I-02** | **GIMP PDB调用** (SDD 4.2.1) | 1. 触发一个“复制图层”的步骤。2. 监控底层PDB调用日志。 | 1. 日志显示调用 `gimp-layer-copy`。2. 参数 `add_alpha=true` 正确传递。3. 返回值为有效图层ID。 | | |
| **I-03** | **状态同步监听** (SDD 4.2.4) | 1. 在GIMP中切换不同的图层。2. 观察插件日志或内部状态。 | 1. 插件在 ≤300ms 内接收到 `layer_changed` 事件。2. UI上的“当前图层”显示更新为新图层名。 | | |
| **I-04** | **配置读写** (SDD 4.3.4) | 1. 在插件设置中修改API Key。2. 点击保存。3. 重启GIMP。 | 1. 配置文件 `config.json` 内容更新。2. 敏感信息（API Key）非明文存储（或存储于系统凭据库）。3. 重启后配置保持不变。 | | |

---

### **3.5 文档与介质一致性测试**

**测试目标：** 确保发布介质与文档一致。

| 序号 | 需求规格 | 操作步骤 | 期望输出 | 实际输出 | 通过与否 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **D-01** | **安装手册一致性** | 1. 按照《用户手册》的安装步骤操作。2. 将插件文件放入 `plug-ins` 目录。 | 1. 手册步骤与实际目录结构一致。2. 无需额外配置即可在GIMP菜单中看到插件入口。 | | |
| **D-02** | **权限说明验证** | 1. 检查安装包文件权限。2. 检查插件运行时对 `config` 目录的读写。 | 1. 安装包不包含不必要的可执行权限。2. 插件能正常创建日志文件和配置文件，无权限拒绝错误。 | | |
| **D-03** | **日志与诊断** (SDD 3.4.2) | 1. 触发一个已知错误（如断网点击分析）。2. 点击UI上的“一键复制日志ID”。 | 1. 日志文件 `gimp-ai-mentor.log` 生成且包含错误堆栈。2. 复制的ID能在日志文件中快速检索定位。 | | |

---

### **3.6 自由操作测试**

**测试目标：** 模拟用户异常操作，验证系统鲁棒性。

| 序号 | 需求规格 | 操作步骤 | 期望输出 | 实际输出 | 通过与否 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **E-01** | **异常输入容错** | 1. 在输入框输入超长字符（>1000字）。2. 输入特殊字符（SQL注入/XSS脚本片段）。 | 1. 超长字符被截断或提示“长度限制500字符”。2. 特殊字符被转义显示，未导致UI错乱或脚本执行。 | | |
| **E-02** | **GIMP状态突变** | 1. 在AI分析过程中，关闭当前图片窗口。2. 在预览状态下，手动删除预览图层。 | 1. 插件检测到图像关闭，自动清理资源并退出。2. 检测到图层丢失，提示“预览已失效，请重新生成”，不报错。 | | |
| **E-03** | **并发与中断** | 1. 快速连续点击“分析”按钮5次。2. 在AI请求过程中点击“取消”。 | 1. 系统仅发起1次有效请求，其余被防抖机制过滤。2. 点击取消后，请求中断，UI恢复空闲状态。 | | |

---

### **5.4 测试实施与缺陷跟踪**

**1. 缺陷生命周期管理：**
*   **入库 (Submit)：** 测试人员提交缺陷，附带日志ID、截图、复现步骤（必填）。
*   **分配 (Assign)：** 测试组长审核后分配给开发人员。
*   **解决 (Resolve)：** 开发人员修复代码，提交版本库，状态置为“已修复”。
*   **验证 (Verify)：** 测试人员在新版本中验证缺陷是否关闭。
*   **归零 (Close)：** 验证通过，缺陷关闭；验证失败，重新打开并指回开发。

**2. 回归测试策略：**
*   **每日构建 (Daily Build)：** 每次代码提交后，自动运行冒烟测试（F-01, F-03）。
*   **版本发布前：** 必须执行全量测试（功能+性能）。
*   **缺陷修复后：** 必须对修复的缺陷进行验证，并对其关联模块进行回归测试（例如修复了“配方导入”，需重测“配方导出”和“执行”）。

**3. 测试结论：**
*   测试结束后，出具测试报告，统计通过率、遗留缺陷列表。
*   只有当所有P0级用例通过且无严重缺陷时，方可签署发布确认书。
